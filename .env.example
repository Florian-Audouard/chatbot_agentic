OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini
# Optional OpenRouter settings
# OPENROUTER_API_BASE=
# OPENROUTER_APP_URL=
# OPENROUTER_APP_TITLE=

# OpenWebUI tip: add custom header `x-thread-id: {{chat_id}}` in the connection settings
# so the server can reuse thread memory per chat.
